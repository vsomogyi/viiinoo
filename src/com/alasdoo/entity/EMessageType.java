package com.alasdoo.entity;

/**
 * Messages sent to pages can have different types:
 * ERROR,INFO,SUCCESS.
 * The type defines the css which will be used on the page to display the message.
 * @author tturzai
 *
 */
public enum EMessageType {
	ERROR, INFO, SUCCESS;
}
